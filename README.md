# Simple AutoScript V2Ray & Xray core

### Installation
```
apt-get update -y && apt-get upgrade -y && wget -q https://raw.githubusercontent.com/AxFrds/AutoScript-V2RAY-XRAY/master/setup.sh && chmod +x setup.sh && ./setup.sh && rm -f setup.sh
```
